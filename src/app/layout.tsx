import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Jetsy Ads Platform - Campaign Management Platform",
  description:
    "Create and manage Meta (Facebook & Instagram) advertising campaigns with ease",
  keywords:
    "Ads Platform, Facebook Ads, Instagram Ads, Campaign Management, Digital Marketing",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
